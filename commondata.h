#ifndef COMMONDATA
#define COMMONDATA

//lookup tables, enums etc. needed everwhere

namespace CommonData {
    const int defaultChunkDimensions = 32;

    enum Side{
        TOP,
        BOTTOM,
        RIGHT,
        LEFT,
        FRONT,
        BACK
    };
}

#endif // COMMONDATA
